# Permitir .gitignore
!.gitignore

# Ignorar todo por defecto
*

# Permitir terraform-aws
!terraform-aws/
!terraform-aws/.gitkeep
!terraform-aws/*.tf
!terraform-aws/terraform.tfvars

# Permitir kubernetes-local
!kubernetes-local/
!kubernetes-local/.gitkeep
!kubernetes-local/*.yaml

# Permitir documentación
!documentacion/
!documentacion/*.drawio
!documentacion/*.docx
!documentacion/*.pdf

#gestión de incidentes
!gestion-incidentes/
!gestion-incidentes/.gitkeep
!gestion-incidentes/*.tf
!gestion-incidentes/*.tfvars
!gestion-incidentes/*.txt

# Ignorar archivos generados
terraform-aws/.terraform/
terraform-aws/terraform.tfstate
terraform-aws/terraform.tfstate.backup
terraform-aws/.terraform.lock.hcl
terraform-aws/*.tfplan
terraform-aws/*.log
.kube/
minikube/
